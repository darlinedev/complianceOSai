{
  "name": "compliance-os",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:web": "pnpm --filter web dev",
    "dev:api": "pnpm --filter api dev",
    "dev": "pnpm --parallel dev"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
